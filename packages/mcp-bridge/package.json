{
  "name": "@genoffice/mcp-bridge",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "description": "Built-in MCP server bridge (stdio + streamable HTTP) for 方塘Office: admin-defined servers from fangtang-mcp.json exposed to the AI agent over IPC. Main process only.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  }
}
